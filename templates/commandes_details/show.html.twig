{% extends 'base.html.twig' %}

{% block title %}CommandesDetails{% endblock %}

{% block body %}
    <h1>CommandesDetails</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commandes_detail.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ commandes_detail.quantite }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ commandes_detail.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commandes_details_index') }}">back to list</a>

    <a href="{{ path('app_commandes_details_edit', {'id': commandes_detail.id}) }}">edit</a>

    {{ include('commandes_details/_delete_form.html.twig') }}
{% endblock %}
