{% extends 'base.html.twig' %}

{% block title %}CouponTypes index{% endblock %}

{% block body %}
    <h1>CouponTypes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for coupon_type in coupon_types %}
            <tr>
                <td>{{ coupon_type.id }}</td>
                <td>{{ coupon_type.name }}</td>
                <td>
                    <a href="{{ path('app_coupon_types_show', {'id': coupon_type.id}) }}">show</a>
                    <a href="{{ path('app_coupon_types_edit', {'id': coupon_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_coupon_types_new') }}">Create new</a>
{% endblock %}
